describe("First test", () => {
  let a, b;
  it("shows that a is true", () => {
    a = false;
    expect(a).toBe(true);
  });
});

describe("First test", () => {
  let a;
  it("shows that a is true", () => {
    a = true;
    expect(a).toBe(true);
  });
});